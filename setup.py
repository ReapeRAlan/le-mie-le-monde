from setuptools import setup, find_packages

setup(
    name="yolov5",
    version="0.1",
    packages=find_packages(),
    # O puedes usar 'py_modules' si prefieres
    # py_modules=['module1', 'module2', ...]
)
